"""Repository package for MLaNDS.

The repository layer is responsible for loading and validating external data,
particularly the approved-device list stored in JSON. Centralising this logic in
one package makes validation and maintenance easier while keeping the rest of
application logic focused on behaviour rather than file parsing.
"""
